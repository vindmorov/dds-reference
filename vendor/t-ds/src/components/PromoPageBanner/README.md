# PromoPageBanner

Крупный визуальный блок, выполняющий роль хедера (шапки) для промо-страниц, онбордингов и других контекстных страниц банка. Предназначен для создания первого яркого впечатления, донесения главного сообщения страницы и задания общего тона коммуникации.

## Props

### Контент и варианты

| Проп | Тип | По умолчанию | Описание |
|---|---|---|---|
| `title` | `React.ReactNode` | `'Text 5XL'` | Заголовок. Размер переключается автоматически: 5XL на десктопе, 3XL на адаптиве (`useIsMobile`) |
| `description` | `React.ReactNode` | `'Text XL'` | Описание. Размер переключается автоматически: XL на десктопе, M на адаптиве (`useIsMobile`) |
| `buttonLabel` | `React.ReactNode` | `'Text M'` | Текст кнопки действия |
| `image` | `React.ReactNode` | — | Произвольный элемент-изображение |
| `imageSrc` | `string` | — | URL изображения (приоритетнее `image`) |
| `imageAlt` | `string` | `''` | Alt-текст изображения |
| `className` | `string` | `''` | Дополнительные CSS классы |

### Показ/скрытие элементов

| Проп | Тип | По умолчанию | Описание |
|---|---|---|---|
| `hasImage` | `boolean` | `true` | Показывает блок с изображением |
| `hasDescription` | `boolean` | `true` | Показывает описание |
| `hasButton` | `boolean` | `true` | Показывает кнопку действия |
| `hasNavigation` | `boolean` | `true` | Показывает или скрывает навигацию внутри баннера |
| `imageOverflow` | `'hidden' \| 'visible'` | `'hidden'` | Управляет вылетом изображения за границы баннера |
| `imageObjectFit` | `'contain' \| 'cover'` | `'contain'` | Способ вписывания изображения |
| `imageObjectPosition` | `string` | — | Позиция изображения внутри блока |

### Состояние

| Проп | Тип | По умолчанию | Описание |
|---|---|---|---|
| `onButtonClick` | `() => void` | — | Колбэк при нажатии на кнопку |
