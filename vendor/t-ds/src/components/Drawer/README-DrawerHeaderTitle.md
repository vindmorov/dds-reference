# DrawerHeaderTitle

Внутренний компонент заголовка для `DrawerHeader` с выбором размерного варианта.

## Props

- `children` — содержимое заголовка
- `variant` — `text-m` или `text-l`
- `className` — дополнительные CSS-классы
