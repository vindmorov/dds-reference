# Input

Текстовое поле ввода с поддержкой подписи, подсказки, сообщения об ошибке и боковых аксессуаров.

## Props

### Контент и варианты

| Проп | Тип | По умолчанию | Описание |
|---|---|---|---|
| `label` | `string` | — | Подпись над полем |
| `placeholder` | `string` | — | Текст-заглушка при пустом поле |
| `type` | `'text' \| 'password' \| 'email' \| 'tel' \| 'url'` | `'text'` | Тип нативного поля. Не применяется вместе с `format` |
| `variant` | `'default' \| 'white'` | `'default'` | Цветовая тема поверхности поля |
| `description` | `string` | — | Вспомогательный текст под полем |
| `errorMessage` | `string` | — | Текст ошибки (заменяет `description` при `isError=true`) |
| `left` | `React.ReactNode` | — | Произвольный элемент слева от поля ввода |
| `right` | `React.ReactNode` | — | Произвольный элемент справа от поля ввода |
| `format` | `'number' \| 'currency' \| 'percent' \| 'phone'` | — | Форматирование числового значения или телефонная маска `+7` |

### Состояние

| Проп | Тип | По умолчанию | Описание |
|---|---|---|---|
| `value` | `string` | — | Управляемое значение. Если передан — компонент переходит в controlled mode |
| `onChange` | `(value: string) => void` | — | Колбэк при изменении значения |
| `isDisabled` | `boolean` | `false` | Блокирует поле |
| `isError` | `boolean` | `false` | Переводит поле в состояние ошибки |
